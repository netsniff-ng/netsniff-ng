Some future ToDos
=================

 * Better visualization of packets during debug-output
 * External tool for BPF filter code generation
 * Support for data export in pcap file format
 * Deep Packet Inspection
 * Add TX_RING and dynamically choose rings during runtime (RX, TX, both)
 * Support for setting CPU affinity
 * Kernel patch for loading more than one BPF code to a single socket
 * Regexp on packet content
 * ... 
